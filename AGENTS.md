# Human Composer

## 项目介绍

Human Composer 是一个个人多行动并行管理器，用可视化图形界面编排多个项目及其交付目标的并行执行流程，帮助用户明确「现在该做什么」，以实现效率最大化。

### 设计哲学

1. **Minimum effort** — 用户输入以语音/自然语言为主；能自动化就自动化，能直观呈现就直观呈现。
2. **e/acc** — 有效加速，帮助用户实现效率最大化。
3. **前额叶保护** — 用户的前额叶资源有限，软件的核心效用是解放前额叶、延长前额叶在线时间；一切设计决策都应减少用户的认知负荷。
4. **细粒度、低阻力** — 专注管理碎片化的具体行动，高触发频率要求记录和反馈流程极简。

#### 交互原则（由设计哲学推导）

- 每个高频操作 ≤ 2 步完成（minimum effort）
- 系统主动呈现可执行洞察，而非被动展示数据（e/acc）
- 完成行动后零认知间隙：自动推荐下一个（前额叶保护）
- 无确认弹窗，误操作靠 Undo 挽回（细粒度、低阻力）

### 技术栈

- **桌面端**: Tauri 2 (Rust)
- **前端**: React + TypeScript + Vite
- **图形编排**: React Flow (`@xyflow/react`)
- **目标平台**: macOS（早期）

### 核心功能（规划）

- **今日安排视图**：跨项目聚合的时间轴式日程，作为应用默认首页，回答「从现在到今天结束该做什么」
- 图形化行动编排界面：展示并行目标、行动及其关系（block、顺序）
- 三层交互面：菜单栏（零成本 glance）→ 常驻悬浮窗（日常执行主界面）→ 主窗口（规划编排）
- 完成→推荐循环：完成行动后自动推荐下一个，零认知间隙
- Inbox 行动收集：快速输入，自动分配到对应目标
- 菜单栏指示器：快速查看当前应执行的行动
- 多 project 源：支持手动创建，预留解耦接口供外部软件同步

### 术语表（Glossary）

| 用户可见（中文） | 代码标识 | 持久化（DB/IPC） | 含义 |
|-----------------|----------|------------------|------|
| 项目 | `Project` | `projects` | 顶层容器 |
| 目标 | `Outcome` | `branches` / `branch_id` | 项目内可并行推进的、可衡量的交付成果（类 OKR 的 KR） |
| 行动 | `Action` | `tasks` / `task_id` | 为达成目标而执行的具体步骤（几分钟～半小时） |
| 泳道 | `DayLane` | `day_lanes` | 今日注意力线程，可跨项目混排行动；≠ 目标 |
| Inbox | — | `status = inbox` | 尚未归属任何目标的行动 |

**边界**：目标回答「这个项目要并行推进哪些成果？」；泳道回答「我今天用哪几条注意力线程执行？」

---

## 项目规范

### Git 分支策略

采用 **Gitflow**：

| 分支 | 用途 |
|------|------|
| `main` | 生产就绪代码 |
| `develop` | 集成分支 |
| `feature/*` | 新功能 |
| `release/*` | 发布准备 |
| `hotfix/*` | 紧急修复 |

### Commit 规范

- 使用 **英文** commit message
- 遵循 [Conventional Commits](https://www.conventionalcommits.org/) 格式，例如：
  - `feat: add task graph editor`
  - `fix: resolve menu bar icon rendering`
  - `chore: update dependencies`

### 提交节奏

- 做了一次**显著修改**后应先提交，即使尚未完全验证闭环
- **按具体改进项拆分提交**：不同功能、修复或文档更新应各自独立 commit，改一项提交一项，不要攒成大杂烩
- 示例：后端死锁修复、悬浮窗 URL 修复、窗体圆角样式应分别为独立 commit

### Devlog 规范

- 根目录维护 [`devlog.md`](devlog.md)
- **每次提交前**先记录本次主要改动，比 commit message 更详细但仍保持简要，确保主要改动都提及
- 新的 devlog 条目应将**上一条** devlog 对应条目的 git commit hash 附上
